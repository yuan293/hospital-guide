# 贡献指南

感谢关注诊途（hospital-guide）。这是一个以"规则守住安全底线、模型接住表达多样性"为核心的开源导诊原型。参与贡献前，请先阅读本指南与 [README](README.md) 的「当前限制」与「维护与开放计划」。

## 欢迎的贡献

- **规则依据勘误**：症状词表、红旗征配置中表述不当或与公开权威依据冲突的条目。Issue 中请附上公开依据来源。
- **科室数据适配**：其他医院/院区的 `data/hospital.json` 配置样例——**仅限虚构数据或已获书面授权的数据**，请勿提交任何真实医院的内部资料。
- **评测案例扩充**：新的口语化主诉、急症场景、红队注入样本。必须是合成内容，**禁止提交真实患者信息**（含他人转述的就诊经历）。
- **跨平台体验**：macOS/Linux 下的启动与模型适配问题反馈与修复。
- **无障碍与适老化**：关怀模式的对比度、字号、读屏（ARIA）兼容性改进。
- **文档与教程**：表述勘误、安装部署问题、复现指南改进。

## 不接受的方向

为守住项目边界，以下 PR 不会被合并：

- 接入真实号源、真实支付或真实患者数据的改动；
- 任何**降低安全门槛**的修改（如放宽红旗征、允许模型覆盖急诊结论、移除证据核验）；
- 引入云端依赖、遥测上报或对外发送病情描述的功能；
- 未经医学专业审核却宣称"临床准确率""可用于真实就医"的表述或配置；
- 把虚构演示数据包装成真实医院资料的内容。

宁可多转人工，也不放行可疑急症，这是本项目不可谈判的原则。

## 开发环境

- Node.js 20 或以上；
- **无需 `npm install`**：项目刻意零第三方运行时依赖，仅使用 Node 内置模块；
- Windows 下 `npm start` 或双击 `start.cmd` 启动，默认监听 `http://127.0.0.1:3210`；
- 涉及模型链路的改动才需要本机 Ollama 与 qwen2.5 双模型（见 README「本地大模型」），普通规则/前端改动不需要。

## 改动后的验证

提交前请至少跑通快速验证链：

```sh
npm test          # 51 个单元测试
npm run data:validate   # 数据结构 fail-closed 校验
npm run verify    # 快速链：单测 + 数据校验 + 无模型评测 + 失败白名单门禁
```

按改动内容还需要：

| 改动类型 | 额外步骤 |
| --- | --- |
| 修改 `data/hospital.json` 的 probes/扩展 | `npm run fhir:export` 重新生成 `StructureDefinition/` 与 `Questionnaire/`，并一并提交 |
| 修改规则、词表、红旗征 | `npm run evaluate`；本机有模型时再跑 `npm run evaluate:models` 与 `npm run redteam:models` |
| 新增鉴别 probe 或红旗征场景 | **必须同时新增评测案例**（无案例的规则改动不予合并） |
| 改动安全相关逻辑 | `npm run verify:full`（完整链，含双模型评测与双模型红队） |

评测结果携带 12 个核心文件的 sha256 指纹：数据变化后旧的 `data/evaluation/latest.json` 会标记过期，这是预期行为，重跑评测即可刷新。**请勿手工编辑评测结果文件去"消除"失败**——失败案例应先分析轨迹，确认是规则缺陷还是标签问题，再决定修复或更新标签。

## 评测案例编写规范

- 案例写入 `data/evaluation/cases.json`，分为 `regression`（已知行为回归）与 `challenge`（扩展压力）两个 suite；
- 只适用于特定模式的案例用 `modes` 字段声明，不适配的组不计入分母；
- 使用 `expected.anyOf` 表达多个等价安全结局时，必须满足 fail-closed 约束（≥2 个结局、只能是 human/uncertain 且 department 为 null），并在 `labelNote` 写清每条路径的真实来源；
- 证据类断言必须是 2–8 字的短症状片段，不得把整句主诉当作证据。

## Issue 与 PR 约定

- Issue 标题请简述现象，正文附复现步骤、预期行为与实际行为；前端问题请附截图与浏览器/Node 版本。
- **请勿在 Issue 或 PR 中附带**：个人就诊记录、身份证号等个人信息、账号密码、API 密钥、`.runtime/` 内的任何文件。
- PR 请保持小而聚焦：一个 PR 解决一个问题，并说明改了什么、为什么这样改、如何验证。
- 提交信息建议使用 `feat:` / `fix:` / `docs:` / `test:` / `chore:` 前缀加简述（可参考仓库提交历史）。

## 许可证

本仓库代码、数据、文档与测试统一以 [Apache-2.0](LICENSE) 发布。提交 PR 即表示你确认贡献内容为本人原创或有权提交，并同意以 Apache-2.0 许可发布。使用了第三方资源（含 AI 辅助生成的代码与文本）的，应在 PR 说明中注明来源，并自行完成许可与权利核查；AI 生成内容不当然构成贡献者的自主知识产权。

## 项目维护说明

本项目由学生团队在竞赛与学习节奏下维护，会随版本迭代集中处理 Issue 与 PR，**不承诺具体响应时限**；安全相关勘误会优先处理。
